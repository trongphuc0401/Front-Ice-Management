import { FC } from "react";

const AppRoutes: FC = () => {
  // const router = createBrowserRouter(useConfigureRoutes());
  // return <RouterProvider router={router} />;
  return <div></div>;
};

export default AppRoutes;
